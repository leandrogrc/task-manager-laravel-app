services:
    app:
        build: .
        ports:
            - "8000:8000"
        restart: unless-stopped
        healthcheck:
            test: ["CMD", "php", "-r", "file_get_contents('http://localhost:8000/up') or exit(1);"]
            interval: 30s
            timeout: 5s
            retries: 3
            start_period: 15s
        environment:
            - APP_NAME=${APP_NAME:-Task Manager Laravel App}
            - APP_ENV=${APP_ENV:-production}
            - APP_KEY=${APP_KEY:-base64:Y08FAprvMtFXbficlXPLjA4fqcdGOif16mwlNO7MmVE=}
            - APP_DEBUG=${APP_DEBUG:-false}
            - APP_URL=${APP_URL:-https://task.leandrogrc.dev}
            - APP_LOCALE=pt_BR
            - APP_FALLBACK_LOCALE=pt_BR
            - APP_FAKER_LOCALE=pt_BR
            - DB_CONNECTION=${DB_CONNECTION:-pgsql}
            - DB_HOST=${DB_HOST:-postgres}
            - DB_PORT=${DB_PORT:-5432}
            - DB_DATABASE=${DB_DATABASE:-tasks}
            - DB_USERNAME=${DB_USERNAME:-postgres}
            - DB_PASSWORD=${DB_PASSWORD:-}
            - SESSION_DRIVER=${SESSION_DRIVER:-file}
            - TRUSTED_PROXIES=${TRUSTED_PROXIES:-*}

        networks:
            - shared_network

networks:
    shared_network:
        external: true